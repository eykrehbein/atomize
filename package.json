{
    "name": "root",
    "private": true,
    "devDependencies": {
        "lerna": "^3.22.0"
    },
    "workspaces": [
        "packages/*",
        "tests/*",
        "docs/website"
    ],
    "scripts": {
        "build": "lerna run build",
        "test": "lerna run scoped-test",
        "lint": "lerna run lint",
        "lerna-publish": "lerna publish",
        "docs": "lerna run docs-start",
        "docs-build": "lerna run docs-build"
    }
}
