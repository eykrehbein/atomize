{
    "name": "root",
    "private": true,
    "devDependencies": {
        "lerna": "^3.22.0"
    },
    "workspaces": [
        "packages/*",
        "tests/*"
    ],
    "scripts": {
        "build": "lerna run build",
        "test": "lerna run scoped-test",
        "lerna-publish": "lerna publish"
    }
}
