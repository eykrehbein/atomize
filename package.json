{
    "name": "root",
    "private": true,
    "devDependencies": {
        "lerna": "^3.22.0"
    },
    "workspaces": [
        "packages/*",
        "tests/*"
    ],
    "scripts": {
        "test": "lerna run scoped-test",
        "publish": "lerna publish"
    }
}
